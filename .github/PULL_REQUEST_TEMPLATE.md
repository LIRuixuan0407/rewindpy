## What changed / 修改内容

Describe the user-visible behavior and why it belongs in RewindPy.
说明用户可见的变化，以及它为什么适合加入 RewindPy。

## How it was tested / 测试方式

- [ ] `python -m ruff check .`
- [ ] `python -m pytest -q`
- [ ] `rewindpy doctor`
- [ ] A generated HTML report was inspected when report behavior changed / 修改报告行为时已人工检查生成的 HTML

## Safety and compatibility / 安全与兼容性

- [ ] Runtime data remains local by default / 运行时数据默认保留在本地
- [ ] New captured values follow redaction and size limits / 新捕获数据遵循脱敏和大小限制
- [ ] Python 3.10 compatibility is preserved / 保持 Python 3.10 兼容
- [ ] English and Chinese user-facing text are both updated / 中英文用户界面和文档均已更新
